El PARSER recibe los tokens y agrupa los tokens en comandos

Además se indica donde redirigir cada comando.

Aquí se crea la lista de árboles binarios y se agrupan los comandos
juntando todo lo que haya hasta un pipe o redirección como un mismo comando.


Dejar un espacio si hay espacios entre argumentos

El output que produce son todos los comandos agrupados con la información
de cual es el input que toma y a donde redirige su output y este es el
input que toma el executor.

1 agrupar los comandos
2 de cada comando definir a donde se le redirige
3 separar el input y añadirle un espacio cuando sea necesario
4 solo mantener comillas interiores si las exteriores son distintas
5 si hay algo que expandir dentro de comillas simples apuntarlo para
 expandir o no expandir durante la ejecución