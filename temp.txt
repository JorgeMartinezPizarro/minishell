si el nodo actual es tipo pipe entonces crear un pipe de los dos siguientes nodos
tree->left->cmd y tree->right->cmd

primero crear el pipe y luego ejecutar los comandos el primer comando escribe
en el pipe y el segundo 
ejemplo: echo "hola" > outfile | cat >> outfile
resultado en outfile: hola, solo uno porque el cat lee de un pipe vacío

primero redirigimos el echo a el pipe de escritura y luego redirigimos el echo a outfile

en cat redirigimos la entrada al pipe de lectura y redirigimos la salida a outfile

lo necesario entonces es PRIMERO gestionar las redirecciones para cada comando

1 Recorrer recursivamente hacia la izquierda el árbol
2 Si el nodo siguiente es comando se ejecuta

Hay que gestionar los pipes y crear subprocesos.

Para los subprocesos cuando estemos en un nodo con subprocces = true
se hace fork y cuando ya haya ejecutado la rama derecha -> return
y en el nodo superior se pone un waitpid.

Cuando estamos en un nodo pipe se crea un pipe y recorremos hacia la izquierda
hasta que en la izquierda haya un comando

Se necesitan tres cosas:
1 Hacer fork para cada comando con pipe a su derecha
2 

Comandos que se ejecutan en el padre: comandos execve que en su nivel superior
no tengan un pipe, esto se puede conseguir con una flag 